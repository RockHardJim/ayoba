$(function(){
    "use strict";

    var server = 'https://ayoba.tdigital.dev';
    var loginurl = '/login';
    var registerurl = '/register';

    $(document).ready(function() {
        isRegistered();
    });

    function isRegistered(){
        if(window.location.href != server + loginurl || window.location.href != server + loginurl){

        } else {

        }
    }
});